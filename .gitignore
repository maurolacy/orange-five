# VS
.vscode/

# Node
node_modules/

# Debug output (traces, view PNGs) — disposable
harness/debug/
.ofd-out/

# Signed HLS URLs — expiring, local-only debug material (never commit)
harness/urls.txt

# OS noise
.DS_Store
